{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-linux",
            "type": "shell",
            "command": "${env:HOME}/.dotnet/dotnet",
            "args": [
                "build",
                "PyoroGL/PyoroGL.csproj"
            ],
            "problemMatcher": "$msCompile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
